

<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppTheme" parent="Theme.NextGIS.AppCompat.Light">
    </style>

    <style name="AppTheme.Dark" parent="Theme.NextGIS.AppCompat.Dark">
    </style>

    <style name="menu_labels_style">
        <item name="android:background">@drawable/abc_popup_background_mtrl_mult</item>
        <item name="android:textColor">@color/primary_text_default_material_light</item>
    </style>

    <style name="status_panel">
        <item name="android:textViewStyle">@style/status_panel_text</item>
    </style>

    <style name="status_panel_text"
           parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:maxLines">1</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:drawablePadding">2dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="status_panel_text_land"
           parent="status_panel_text">
        <item name="android:singleLine">true</item>
    </style>

    <style name="IntroTip" parent="TextAppearance.AppCompat.Medium">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/color_white</item>
    </style>
</resources>